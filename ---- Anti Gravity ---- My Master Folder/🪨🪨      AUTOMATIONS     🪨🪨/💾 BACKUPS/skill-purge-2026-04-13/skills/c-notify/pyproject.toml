[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "ltc-runner"
version = "2.0.0"
description = "Low Token Challenge game engine"
requires-python = ">=3.10"

[project.scripts]
ltc = "ltc-runner:main"
